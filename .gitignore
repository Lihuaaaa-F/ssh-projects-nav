# ── 个人隐私文件:严禁入库 ──
# 这些文件含真实用户名 / 项目路径 / SSH 会话别名,仓库里绝不能出现

# 本地私有配置(扫描目录、排除路径、路径别名)
**/ssh-projects-nav.config.ps1

# 项目库 / 手动清单(含 48+ 个真实项目路径)
**/.oc-projects.tsv
**/.oc-manual.txt

# 临时产物
*.oc.tmp.*