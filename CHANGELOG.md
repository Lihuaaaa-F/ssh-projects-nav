# 更新日志

本项目所有值得记录的变更。

## [0.2.0] - 2026-08-11

### 修复

- **中文乱码(SSH 场景)**:Windows PowerShell 5.1 在无控制台/管道输出(SSH、重定向)时会把 `Write-Host` 内容序列化为 UTF-16,任何终端都无法正确显示。
  新增字节直写输出(`Out-NavLine` / `Out-Term`),所有用户可见中文按 UTF-8 字节输出,SSH 终端下完全正常。
- **脚本文件编码损坏**:历次编辑把部分中文字符混写成 GBK 字节 / U+FFFD / 行内 BOM 杂质,导致配置解析报错、输出乱码。
  整体重建 `Microsoft.PowerShell_profile.ps1`;清除 `launch.ps1`、`ssh-projects-nav.config.ps1` 的行首 BOM 杂质字符。
- **启动报错**:`ssh-projects-nav.config.ps1` 首行混入 BOM 字符,`(不入库)` 被当成命令执行而报错;已清除,配置正常加载。
- **cx 命令报错**:`cx.cmd` 残留错误重定向 `chcp 65001 >nob`,每次执行都输出错误;已修正为 `>nul`。
- **列表杂音**:`Save-ProjectTsv` 的回传值被误打印到屏幕,`go` / `go-refresh` 时会刷出整屏 TSV 行;已过滤。

### 变更

- **`go` 每次都全量重扫**:移除 10 分钟缓存,`go` 列出项目前先重新扫描(约 1~2 秒),新增项目立即可见,不再出现"刚建的项目列表里找不到"。
- **列表对齐**:key 按显示宽度对齐(中文按 2 列计宽),路径开头逐列对齐,含中文 key 的项目也能对齐。
- **恢复 `cx` 函数**:重建过程中恢复 profile 交互层的 `cx` 命令(进入项目并启动 Codex)。

## [0.1.0] - 2026-08-10

- 初始版本:SSH 远程项目导航器,`go` 列出 → 复制一行 → `oc`/`cl`/`cx` 进入并启动对应 AI 工具。